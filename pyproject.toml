[project]
name = "cudos"
version = "0.1.0"
description = "Postgres 17 mocker"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sqlglot>=30.18.0",
]

[project.optional-dependencies]
test = [
    "asyncpg >=0.31",
]
